/vendor
/build
composer.lock
.phpunit.result.cache
.phpunit.cache
.DS_Store
/node_modules
/tests/__fixtures__/storage

# Written by `npm run dev`. Committing it points every installing site's Control
# Panel at a dev server that does not exist, so it loads no addon assets at all.
/dist/hot
